spack:
  specs:
    - cmake
    - git
    - tmux
  view:
    base:
      root: ${HOME}/bin/spack-core
      projections:
        openssl: '{name}/{version}'
  concretizer:
    targets:
      granularity: generic
  packages:
    openssl:
      require: "certs=system"

