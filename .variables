# fix annoying completion issue in rhel7
shopt -u progcomp
set -o vi

# file permissions
umask u=rwx,go=rx

export SOFT_DIR=${HOME}/soft
#export PATH=${HOME}/bin/spack-core/bin:${HOME}/bin/spack-editor/bin:${PATH}
export PATH=${HOME}/bin/dotfiles-bin:${PATH}
export EDITOR=nvim
export MYCONFIGINSTALLED=1
export TERM=xterm-color
