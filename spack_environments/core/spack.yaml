spack:
  specs:
  - cmake
  - git
  - tmux
  - llvm
  view:
    base:
      root: ${HOME}/bin/spack-core
      projections:
        openssl: '{name}/{version}'
      link: roots
  concretizer:
    targets:
      granularity: generic
  packages:
    openssl:
      require: certs=system
